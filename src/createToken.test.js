// const createToken = require('./createToken');

// const output = "Success!";

// jest.mock("./createToken", () => ({
//         handler: () =>
//         output

//       }) 
// );

// // test ('create token is executed', async () => {
// //     expect(createToken.handler()).toEqual(output)
// // });

// // test ('check user token is valid', async () => {
// //     expect(createToken.handler().toEqual(output))
// // });
